# clip-core-foundations

Implement clip-core's domain models, MIME normalization, content hashing, search-query parsing, and app
config - the shared foundation every other crate in the workspace depends on. First change in the PRD's
build order.
